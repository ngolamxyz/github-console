module.exports = {
  options: {
    verbose: true,
    enableReactRefresh: true
  },
  plugins: ['scss'],
  experimental: {
    // Enables the styled-components SWC transform
    styledComponents: true
  }
};